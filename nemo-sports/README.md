# NEMO Sports · نيمو سبورتس

**Live Sports, Every Moment** — منصة رياضية شاملة متعددة الرياضات (8 رياضات) بنيت على مواصفات
المشروع الشاملة: نتائج مباشرة، أخبار، بطولات، فرق، لاعبون، بث رسمي مرخّص فقط، ولوحة تحكم.

نسخة تنفيذية (Next.js 15 + Tailwind 4 + TypeScript) تعمل ببيانات توضيحية مولّدة محليًا.

---

## التشغيل

```bash
npm install
npm run dev      # تطوير  → http://localhost:3000
npm run build    # بناء إنتاجي (209 صفحة)
npm run start    # تشغيل نسخة الإنتاج
npm run check    # فحص شامل لكل المسارات مقابل خادم يعمل
```

> `npm run check` يفترض وجود خادم على `http://127.0.0.1:3000` — يمكن تغييره بـ
> `BASE=http://… npm run check`.

---

## متغيرات البيئة

كل المتغيرات اختيارية — المنصة تُقلع بدونها وتتدهور بدل أن تنهار. انظر
`.env.example` للتفاصيل.

| المتغير | ماذا يفتح |
|---|---|
| `NEXT_PUBLIC_SITE_URL` | أصل الإنتاج الموحّد (canonical، Open Graph، JSON-LD، sitemap، robots). الافتراضي: `https://nemo-sports.vercel.app` — اضبطه مرة واحدة عند ربط دومين مخصص فعليًا |
| `NEXT_PUBLIC_DEMO_CONTENT` | `1` لإظهار بيانات العرض التجريبي في بناء إنتاجي (لعروض الديمو فقط — يبقى noindex). في الإنتاج العادي البيانات التجريبية مخفية وتظهر حالات «البيانات غير متوفرة» الصادقة |
| `SPORTRADAR_KEY` · `SPORTMONKS_TOKEN` · `API_FOOTBALL_KEY` · `THESPORTSDB_KEY` | تفعيل المزوّد في سلسلة الأولويات؛ وأول مفتاح ينقل الوضع من `demo` إلى `live` فيعيد الفهرسة |
| `DATABASE_URL` | المخزن الكنسي الدائم في Postgres (بدونه: ذاكرة داخل العملية) |
| `REDIS_URL` | طبقات الكاش الخمس مشتركة بين النسخ (بدونه: LRU داخل العملية) |
| `NEMO_INGEST_TOKEN` | مهمة الاستيعاب؛ بدونه يرفض `/api/v1/ingest` بالعمل (503) حتى لا يُصرف رصيد مدفوع بلا حراسة |
| `FOOTBALL_DATA_API_KEY` | مفتاح **Football-Data.org** (من لوحة المزوّد) — يُقرأ على السيرفر فقط. عند ضبطه يقود المزوّد سلاسل الترتيب والهدافين والمباريات |
| `FOOTBALL_DATA_REQUESTS_PER_MINUTE` | سقف الطلبات الداخلي للمزوّد (افتراضي `8`، والخطة المجانية تسمح بـ`10/دقيقة`). ارفعه للخطط المدفوعة |
| `FOOTBALL_DATA_COMPETITIONS` | البطولات المراقَبة في الطلبات غير المقيَّدة (افتراضي: الدوريات الكبرى + الأبطال) |
| `FOOTBALL_DATA_SEASON` | تثبيت موسم معيّن بصيغة سنة البداية (`2025`). الافتراضي: الموسم الحالي |
| `FOOTBALL_DATA_BASE_URL` | تجاوز عنوان الـBase URL (للتطوير مقابل mock محلي فقط؛ اتركه فارغًا في الإنتاج) |
| `NEMO_SDL_MODE` | `auto` (افتراضي) أو `demo` لإجبار المحوّل التجريبي |

```bash
cp .env.example .env.local   # ثم املأ ما لديك
npm run db:setup             # يتطلب psql + DATABASE_URL
npm run ingest football      # دورة استيعاب واحدة
```

---

## 1. الهوية البصرية

### الاسم والتموضع
| | |
|---|---|
| الاسم | **NEMO Sports / نيمو سبورتس** |
| النطق | NEH-moh — قصير، حاد، سهل التذكر |
| الشعار الفرعي | *Live Sports, Every Moment* |
| التموضع | مصداقية شبكة إعلامية (ESPN / Sky) + سرعة منصة نتائج (FlashScore) |
| نبرة الصوت | رسمي ودود · واثق · مهني · عالمي |

### اللوجو
Monogram «N» مبني من عمودين وقطر يتجاوز الإطار:

- **العمودان** = سلطة المذيع وثباته.
- **القطر الذهبي** = لحظة الحركة (التمريرة، الاختراق، صافرة النهاية).

قواعد صارمة: بلا تدرجات لونية، بلا رموز رياضية مباشرة (كرة/شبكة)، بلا حدود خارجية (strokes).
يعمل من 16px (favicon) حتى اللافتات. ثلاث نسخ: Navy / Gold / White.

الملفات: `components/brand/Logo.tsx` (Mark + Horizontal lockup) · `app/icon.svg` · `brand/nemo-logo.svg`.

### الألوان

| الدور | الاسم | HEX | الاستخدام |
|---|---|---|---|
| أساسي | Navy Dark | `#0F1B2E` | الهيدر، الأقسام الرئيسية، البطاقات الداكنة |
| أساسي أعمق | Navy Ink | `#050B15` / `#0A1424` | شريط الأدوات، خلفية الوضع الداكن |
| لمعة | Gold | `#D4AF37` | التمييز، الأزرار الأساسية، الخطوط الرفيعة |
| تنبيه | Live Red | `#E02D3C` | مؤشر LIVE، الأخبار العاجلة، الهبوط |
| نجاح | Win Green | `#0F9D6A` | الفوز، الحالات السليمة |
| تحذير | Warn Amber | `#D98324` | الحظر الجغرافي، الإصابات |
| محايد | Ink / Muted / Line | `#0F1B2E` · `#5D6B7F` · `#E2E8F0` | نصوص وحدود |
| خلفية | BG / Surface | `#F4F6F9` · `#FFFFFF` | الصفحة والبطاقات |

الوضع الداكن: `--color-bg #070E1A` · `--color-surface #101D30` · `--color-line #1D2C42`.
الذهبي يُستخدم بالتقطير لا بالغمر: خط رفيع، رقم، أو زر واحد في كل شاشة.

### الخطوط
- **النص:** IBM Plex Sans Arabic ← Segoe UI ← system (16px / line-height 1.5).
- **الأرقام والعناوين اللاتينية:** Barlow Condensed (مكثّف، طابع إعلامي رياضي) مع
  `font-variant-numeric: tabular-nums` و`unicode-bidi: isolate` حتى تظل الأرقام LTR داخل RTL.
- H1 32 / H2 28 / H3 24 / H4 20 · eyebrow صغير بحروف متباعدة `letter-spacing .16em`.

### الشكل والمسافات
- **radius:** 6px للبطاقات، 3px للأزرار والشارات — حاد ومهني، لا زوايا دائرية ناعمة.
- **spacing:** 4 · 8 · 12 · 16 · 24 · 32 · 48 · 64.
- **breakpoints:** 360 · 768 · 1024 · 1440+.
- **LIVE indicator:** نقطة حمراء 7px مع هالة نابضة 1.6s + كلمة LIVE.
- **شريط النتائج:** تمرير أفقي بدون scrollbar، يتوقف عند hover، ويحترم `prefers-reduced-motion`.

مرجع بصري واحد الملف: `design/design-system.html` (يفتح مباشرة في المتصفح).

---

## 2. البنية

```
app/
  layout.tsx            RTL + lang=ar + ثيم + Organization/WebSite JSON-LD
  page.tsx              الرئيسية (مباراة اليوم، مباشر، اليوم، قادمة، نتائج، أخبار، بطولات)
  matches/              القائمة + الفلاتر + [slug] تفاصيل المباراة (5 تبويبات)
  live/ results/ fixtures/ standings/
  competitions/[slug]/  ترتيب · مباريات · هدافون · فرق · أخبار
  teams/[slug]/         نظرة عامة · لاعبون · مباريات · ترتيب · أخبار
  players/[slug]/       نبذة · إحصائيات · مباريات · مقارنة · أخبار
  news/ + [slug]/       قائمة بفلاتر + مقالة كاملة (NewsArticle JSON-LD)
  watch/                جدول البث الرسمي + سجل النواقل
  search/ account/ about/ contact/ faq/
  broadcast-rights/ privacy/ terms/ copyright/
  admin/                8 أقسام: لوحة بيانات، مقالات، مباريات، بطولات، بث، مستخدمون، إعلانات، SEO
  api/live/             محرك النتائج المباشرة (واجهة الموقع الحالية)
  api/search/           اقتراحات البحث (تطبيع عربي: أ/إ/آ، ى/ي، ة/ه، تشكيل)
  api/v1/               عقد البيانات العام المُصدَّر (live · matches · system)
  sitemap.ts robots.ts not-found.tsx error.tsx
lib/
  sdl-gateway.ts        الباب الوحيد إلى طبقة البيانات (لا مستورد لمزوّد هنا)
  core-data.ts          رياضات · بطولات · فرق · لاعبون
  data.ts               مباريات · أحداث · إحصائيات · تشكيلات · أخبار · ترتيب · هدافون · نواقل
  live.ts               محرك الساعة والنتائج (نقي، بلا حالة مخزّنة)
  filters.ts format.ts nav.ts
packages/sdl/           ★ طبقة بيانات الرياضة — المكوّن الوحيد المسموح له بمخاطبة المزوّدين
db/schema.sql           المخطط الكنسي (Postgres) · db/seed.sql أولويات المزوّدين وحدودهم
```

### طبقة بيانات الرياضة (Sports Data Layer)

`packages/sdl` حزمة TypeScript مستقلة، **صفر تبعيات تشغيلية**، و71 اختبار وحدة:

```
الواجهة ──► lib/sdl-gateway.ts ──► SportsDataLayer
                                     │  أولويات → محوّل → تطبيع → كاش → حدود معدل → كيان كنسي
                                     ├── Sportradar      (x-api-key · soccer v4)
                                     ├── Sportmonks      (Bearer · v3 · include/pagination)
                                     ├── API-Football    (x-apisports-key · v3 envelope)
                                     ├── TheSportsDB     (X-API-KEY · صور وميتاداتا فقط)
                                     └── demo            (بلا مفاتيح · للتشغيل والاختبار)
```

- **لا يخرج أي شكل خاص بمزوّد**: المحوّلات تُعيد أنواع `Normalized*` فقط.
- **المعرّفات الكنسية مقدّسة**: الربط الخاطئ يُصحَّح بنقل الربط لا بحذف الكيان.
- **لا نقطة فشل واحدة**: عند فشل كل المزوّدين تُعرض آخر قيمة صالحة داخل `staleGrace`
  مع `stale: true` بدل الخطأ.
- **كل استدعاء مبرَّر**: كاش ← فحص حدود ← دمج الطلبات المتطابقة ← ثم الشبكة،
  و`costReport()` يسجّل `avoided` أي ما تجنّبناه عمدًا.
- **الصراعات**: النتيجة/الحالة/الترتيب = المزوّد الأساسي يفوز، وما يُرفض لا يُكتب أبدًا
  بل يبقى في طابور المراجعة حتى قرار المحرّر.
- التوثيق الكامل وطريقة إضافة مزوّد أو نوع بيانات جديد: `packages/sdl/README.md`.

### تكامل Football-Data.org (المصدر المجاني الموثوق لكرة القدم)

المزوّد مُسجّل في طبقة البيانات (`packages/sdl/src/adapters/football-data.ts`)
ويُفعَّل تلقائيًا عند وجود `FOOTBALL_DATA_API_KEY`، بلا أي تغيير في الكود:

```
الصفحات / واجهات API ← lib/football-data.ts ← lib/sdl-gateway.ts ← SDL
                      ← FootballDataAdapter ← https://api.football-data.org/v4
```

| البند | التنفيذ |
|---|---|
| Base URL | `https://api.football-data.org/v4` |
| التوثيق | ترويسة `X-Auth-Token` فقط — لا يظهر المفتاح أبدًا في رابط أو سجل أو استجابة |
| مكان المفتاح | متغيّر بيئة على السيرفر (`FOOTBALL_DATA_API_KEY`) يُقرأ في `getSdl()`؛ لا واجهة عميل ولا حزمة متصفح تراه (يفحصه `npm run check`) |
| الترتيب | `GET /competitions/{code}/standings` — يُعرض جدول `TOTAL` فقط ولا تُدمج جداول HOME/AWAY |
| المباريات والنتائج | `GET /matches?date=YYYY-MM-DD` (و`dateFrom/dateTo` افتراضيًا) + `GET /competitions/{code}/matches` |
| الهدافون | `GET /competitions/{code}/scorers` — مورد يتطلب خطة تتضمّن بيانات الهدافين؛ على الخطة المجانية يعيد المصدر `403` فنعرض «البيانات غير متوفرة» بدل أي أسماء مُخترعة |
| البطولات | الكود الرسمي (`PL`, `PD`, `SA`, `BL1`, `FL1`, `CL`, …) مع قبول أسماء/اختصارات مثل `english-premier-league` |
| الكاش | طبقان في الـ SDL (استجابة المزوّد + البيانات الكنسية) مع `staleGrace`: جدول كل 15 دقيقة، هدافون كل ساعة، مباريات اليوم كل 5 دقائق — و`meta.cache.ttlSeconds` يعلن الصلاحية في كل استجابة |
| Rate Limiting | عدّاد مشترك في العملية (وفي Redis عند ضبطه): `1/ثانية` و`8/دقيقة` افتراضيًا مع مهلة انتظار وتراجع قبل `429`؛ ويُقرأ رأس `X-Requests-Available-Minute` من المصدر |
| الفشل | `"Data temporarily unavailable"` مع `503` وقائمة فارغة — لا مخطط بديل ولا بيانات وهمية، وأي `stale` يُوسم صراحةً بأنه آخر قيمة صحيحة |
| Attribution | «Powered by Football-Data.org» مع رابط dofollow في أسفل الموقع (`components/ui/PoweredByFootballData.tsx`) وفي `components/data/DataSourceNote.tsx` أسفل كل سطح بيانات |

واجهات الخدمة (Backend):

```bash
GET /api/v1/football-data/standings?competition=PL
GET /api/v1/football-data/matches?date=2026-09-17[&competition=PL]
GET /api/v1/football-data/scorers?competition=PL
GET /api/v1/football-data/status      # هل المفتاح مضبوط؟ كم بقي من الحصة؟ ما سياسة الكاش؟
```

- كل استجابة ناجحة تحمل `meta`: المزوّد، `fromCache`، `stale`، `fetchedAt`،
  `cache.ttlSeconds`، و`attribution` — وكل استجابة فاشلة تحمل الرسالة الثابتة فقط.
- `/api/v1/football-data/status` يعرض الحصة المتبقية (`quota`) وحالة الكاش
  وسقف الطلبات، ولا يعرض المفتاح ولا أي ترويسة تحمله.

**سلوك الحصة (Quota)**: الصفحات لا تُطلق طلبات متوازية عمياء؛ فهي تطلب جداول
الدوريات أولًا ثم قوائم الهدافين، وتشتغل حصيلة الدقيقة الواحدة على الكاش أولًا.
عند نفاد الحصة يتخطى الـ SDL المزوّد ويُكمل بسلسلة البدائل (وليس ببيانات وهمية)،
ثم تُستكمل القوائم الناقصة تلقائيًا في دورة التحديث التالية لأن الجداول مخزّنة
15 دقيقة والهدافين 60 دقيقة.

**تطوير بلا إنترنت**: `node scripts/mock-football-data.mjs` يشغّل محاكيًا محليًا
لـ`v4` على نفس ملفات الـFixtures التي تستخدمها الاختبارات، ويُربط بالموقع عبر
`FOOTBALL_DATA_API_KEY=mock` و`FOOTBALL_DATA_BASE_URL=http://127.0.0.1:4321/v4`.

### محرك النتائج المباشرة
`lib/live.ts` **يشتق** الحالة من الزمن الحقيقي منذ انطلاق المباراة بدل تخزينها:

- كرة القدم: `دقيقة'` · `45'–47' = الاستراحة` · `90+X'` · `انتهت` بعد 94.
- كرة السلة: `الربع n · mm:ss` · التنس: المجموعات · اليد: شوطين · الهوكي/البيسبول/الملاكمة لكل منها ساعة.
- الأحداث تظهر تدريجيًا عندما تصل الدقيقة الخاصة بها — ترى الهدف يحدث أمامك.
- `GET /api/live` (كل الحالات) · `?ids=M101` (مباراة واحدة) · `?scope=rail` (شريط الهيدر).
- العميل يعيد الجلب كل 5 ثوانٍ (4 في صفحة المباراة) دون إعادة تحميل، وعند الفشل تبقى آخر
  نتيجة معروفة على الشاشة.
- في الإنتاج: WebSocket + delta updates + Redis.

---

## 3. الامتثال القانوني (مطبّق في الكود لا في الكلام)

- **لا إعادة بث**: البث إما embed من نطاق الناقل أو رابط خارجي بوسم `rel="noopener noreferrer nofollow"`.
- **الإفصاح**: اسم الناقل + نوع الترخيص + المناطق + حالة البث ظاهرة قبل الضغط.
- **الحظر الجغرافي**: تنبيه صريح بالمناطق المسموح بها.
- **عند عدم وجود ناقل**: لا يُعرض أي زر، بل رسالة «البث غير متاح».
- **سجل الترخيص**: `broadcastPartners` بحالة `approved / pending / rejected` — المعلق لا يظهر للزوار.
- **الأخبار الخارجية**: ملخص أصلي + اسم المصدر + رابط بوسم nofollow (`kind: "external"`).
- **الشعارات**: monograms من تصميمنا، لا شعارات مملوكة للغير.
- **الإعلانات**: مواضع معلّمة + وسم «محتوى برعاية» + قاعدة `rel="sponsored"`.
- `robots.txt` يحجب `/admin` و`/account` و`/api/`.

---

## 4. SEO

- **الدومين الموحّد**: كل عناوين SEO تُشتق من `lib/site.ts` (`NEXT_PUBLIC_SITE_URL` ←
  `VERCEL_PROJECT_PRODUCTION_URL` ← `VERCEL_URL` ← `https://nemo-sports.vercel.app`).
  لا يوجد أي دومين مكتوب يدويًا في الكود — أصل الخطأ القديم الذي جعل Search Console
  يرفض كل الروابط بخطأ «URL غير مسموح به».
- Meta title/description/canonical لكل صفحة (عبر `metadataBase`) · Open Graph ·
  `lang="ar" dir="rtl"` · صفحة البحث `noindex` وغير موجودة في الـsitemap.
- JSON-LD: `Organization` · `WebSite` + `SearchAction` · `SportsEvent` · `NewsArticle` —
  بروابط مطلقة على دومين الإنتاج وحالات أحداث مطابقة لما تعرضه الصفحة.
- `sitemap.xml` ديناميكي وواعٍ لوضع البيانات: يعرض فقط صفحات موجودة فعلًا وتعيد 200 —
  صفحات الكيانات التجريبية (مباريات/فرق/لاعبون/أخبار demo) لا تدخل الـsitemap أبدًا
  لأن slugs تُعاد حسابها من «الآن» عند كل بناء وستتحول إلى 404.
- `robots.txt` يشير إلى sitemap على نفس الدومين، ويمنع `/admin` و`/account` و`/api` فقط.
- الفهرسة مرتبطة بمصدر البيانات (Instruction 6): بناء يعمل على بيانات تجريبية =
  `noindex` على كل الصفحات + sitemap مصغّر للصفحات الثابتة الحقيقية فقط. بإضافة مفاتيح
  المزوّدين ينتقل الموقع تلقائيًا إلى `index, follow` ويتوسّع الـsitemap.
- First Load JS للمشترك 103KB · CSS المنتج ≈ 51KB · لا صور خارجية (لا LCP متعثّر).

---

## 5. ما هو منفَّذ / ما هو غير موصول

| منفَّذ ويعمل | غير موصول بعد |
|---|---|
| كل الصفحات والتبويبات والفلاتر | المصادقة والحسابات و2FA |
| محرك مباشر + شريط نتائج + تحديث دوري | WebSocket ودفع الإشعارات |
| بحث فوري بالعربية + صفحة نتائج | الدفع والاشتراكات وشبكة الإعلانات |
| SEO كامل + sitemap + JSON-LD | تطبيق الهاتف |
| لوحة تحكم بواجهاتها الكاملة (8 أقسام) | توصيل بقية الصفحات بطبقة البيانات (الآن `/live` فقط يُغذَّى من SDL) |
| وضع داكن + RTL + تجاوب + وصولية أساسية | إصدار v2 عند أول تغيير كاسر |
| **طبقة بيانات كاملة**: 4 محوّلات موثَّقة + تطبيع + مطابقة ضبابية + صراعات + كاش + حدود + صحة + فشل تلقائي | تشغيل الطبقة على مفاتيح حقيقية (المحوّلات مكتوبة على الوثائق الرسمية وتنتظر أول تغذية حية للتحقق) |
| **عقد API مُصدَّر** `/api/v1/*` (live · matches · system · ingest) | — |
| **مخطط قاعدة البيانات الكنسي** `db/schema.sql` + `db/seed.sql` | — |
| **Postgres كـ`CanonicalStore`** (`packages/sdl/src/pg-store.ts` + `lib/db/pg.ts`) | تطبيق المخطط على خادم حقيقي (`npm run db:setup`) |
| **Redis كـ`KvStore`** (`packages/sdl/src/redis-store.ts` + `lib/cache/redis.ts`) | خادم Redis فعلي (`REDIS_URL`) |
| **مهمة الاستيعاب** `/api/v1/ingest` + `npm run ingest` (محمية برمز) | جدول cron على الخادم |
| **وسم `noindex` تلقائي في وضع demo** (§Instruction 6) | — |

---

## 6. خطة المراحل

| المرحلة | المدة | المحتوى |
|---|---|---|
| 1 — الأساس | 3 أشهر | ✅ منفَّذ هنا: الرئيسية، المباريات، التفاصيل، الأخبار، البث، لوحة أساسية، بحث |
| 2 — التطوير | 3 أشهر | ✅ طبقة البيانات (SDL) + المخطط الكنسي + `/api/v1` · والمتبقّي: حسابات + إشعارات + مفضلات + WebSocket + تشغيل SDL على مفاتيح حقيقية |
| 3 — التوسع | 3 أشهر | CDN وRedis، إعلانات برامجية، Analytics، API عام، اشتراكات |
| 4 — المستقبل | — | تنبؤات، Fantasy، بودكاست/فيديو، تطبيق هاتف |

---

## 7. الفحص

```bash
npm run sdl:test  # 71 اختبار وحدة لطبقة البيانات (node --test) — تمر كلها
npm run build     # ✓ Compiled successfully · Linting and checking validity of types · 209/209 صفحة
npm run check     # اختبارات SDL ثم ✅ ALL GREEN — 121 مسارًا (منها 40 صفحة تفاصيل من الـ sitemap)
npm run start     # ثم الواجهة على http://localhost:3000
```

`scripts/smoke.mjs` يتحقق من: أكواد HTTP لكل المسارات · شكل استجابة `/api/live` · وجود مباريات
LIVE فعلًا · شريط النتائج · البحث العربي · احتواء الرئيسية على أقسامها · و404 للمسارات غير
الموجود · **وعقد `/api/v1`**: أن البيانات الخارجة كنسية، وأن `meta.source` يعلن المصدر، وأنه
لا تتسرب أي مادة اعتماد (مفاتيح/توكنات) في أي حمولة عامة.
